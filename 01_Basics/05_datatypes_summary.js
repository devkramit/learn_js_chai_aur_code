/**
 * Prints a greeting message.
 * @param {string} name - The name to be greeted.
 */
console.log("Hey! Amit");

/**
 * Adds a number to itself.
 * @param {number} num - The number to be added.
 * @returns {number} The sum of the number and itself.
 */

function toAdd(num) {
    return num + num;
}

console.log(toAdd(10));
